// datatypes  

var companyName="luminar Technolab"

console.log(typeof(companyName))

var employee_count=120;
console.log(typeof(employee_count)) //integer

var isOpen=true
console.log(typeof(isOpen)) // boolean

var rating=5.0
console.log(typeof(rating)) //float



// string, number,boolean, null, undefined

let isActive; //undefined

let isOk=null;

// arithmetic operators(+,-,/,*,%,**)

// logical operators (and, or, not) (&&(and), ||(logical or),!(not))

// relational (<,>,<=,>=,==,!=)

// Assignment (=,+=,-=,*=)

//bitwise operator (&,|,^)